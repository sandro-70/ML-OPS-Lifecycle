[pytest]
testpaths = tests
markers =
    requiere_modelo: prueba que depende del modelo real registrado en MLflow (models:/spam-detector@produccion). No disponible en CI porque mlflow.db y mlruns/ estan en .gitignore; se excluye con -m "not requiere_modelo".
